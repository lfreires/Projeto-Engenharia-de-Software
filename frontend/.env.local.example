# Frontend environment variables. Leave VITE_API_BASE_URL empty during local
# development to use the Vite proxy; on Render use the public backend URL.
VITE_API_BASE_URL=
VITE_PROJECT_ID=proj-demo

# Demonstration token. It must match BEARER_TOKEN in the backend.
VITE_BEARER_TOKEN=dev-token
