# Frontend environment variables (copy to .env.local)
# project_id must match what was used when indexing documents in the Ingestion module
VITE_PROJECT_ID=ecommerce-api

# Bearer token — must match BEARER_TOKEN set in query-service/.env
VITE_BEARER_TOKEN=dev-token
